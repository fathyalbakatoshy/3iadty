# نسخ هذا الملف إلى .env وتحديث القيم
# Copy this file to .env and update the values

# Database Configuration - إعدادات قاعدة البيانات
MONGODB_URI=mongodb://localhost:27017/3ayadty
DB_NAME=3ayadty

# Server Configuration - إعدادات الخادم
PORT=5000
NODE_ENV=development
API_BASE_URL=http://localhost:5000

# JWT Configuration - إعدادات JWT
JWT_SECRET=your_super_secret_jwt_key_here_change_in_production
JWT_EXPIRES_IN=24h
JWT_REFRESH_EXPIRES_IN=7d

# OTP Configuration - إعدادات OTP
OTP_EXPIRES_IN=300000
# OTP expires in 5 minutes (300000 milliseconds)

# File Upload Configuration - إعدادات رفع الملفات
UPLOAD_PATH=uploads/
MAX_FILE_SIZE=5242880
# Max file size: 5MB (5242880 bytes)

# Rate Limiting - تحديد المعدل
RATE_LIMIT_WINDOW_MS=900000
# Rate limit window: 15 minutes (900000 milliseconds)
RATE_LIMIT_MAX_REQUESTS=100

# SMS Configuration (for OTP) - إعدادات الرسائل النصية
SMS_API_KEY=your_sms_api_key_here
SMS_SENDER_NAME=3ayadty

# Email Configuration (optional) - إعدادات البريد الإلكتروني
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USER=your_email@gmail.com
EMAIL_PASS=your_email_password
EMAIL_FROM=noreply@3ayadty.com

# Security - الأمان
BCRYPT_ROUNDS=12
SESSION_SECRET=your_session_secret_here

# Logging - التسجيل
LOG_LEVEL=info
LOG_FILE_PATH=logs/app.log

# CORS Configuration - إعدادات CORS
CORS_ORIGIN=http://localhost:3000

# Pagination Defaults - إعدادات التصفح الافتراضية
DEFAULT_PAGE_SIZE=10
MAX_PAGE_SIZE=100

# Audit Logging Configuration - إعدادات سجل التدقيق
AUDIT_LOG_ENABLED=true
AUDIT_LOG_RETENTION_DAYS=365
AUDIT_LOG_SENSITIVE_DATA=false
AUDIT_LOG_EXCLUDE_PATHS=/health,/favicon.ico
AUDIT_LOG_MEDICAL_RETENTION_DAYS=2555
# Medical records retention: 7 years (2555 days)

# Data Encryption - تشفير البيانات
ENCRYPTION_KEY=your_32_character_encryption_key_here 